<navigation-bar title="P7 - Inconsistent Virtual DOM Diffing" back="{{false}}" />
<view class="Box">
  <text>{{md.fun(var_num)}}</text>
</view>
<wxs module="md">
    var count = 0
    module.exports.fun = function(data){
        console.log("[wxs md fun] count:" + ++count);
        return count
    }
</wxs>
