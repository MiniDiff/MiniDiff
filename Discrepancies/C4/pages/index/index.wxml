<navigation-bar title="C4 - Inconsistent Data Backup Mechanism" back="{{false}}" />
<view class="Box">
  <button bind:tap="tap_handler">{{md.fun(var_num)}}</button>
</view>
<wxs module="md">
    var count = 0
    module.exports.fun = function(data){
        console.log("[wxs md fun] count:" + ++count);
        return count
    }
</wxs>
